apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: taskify
  labels:
    app: grafana
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30301
    protocol: TCP
    name: http
