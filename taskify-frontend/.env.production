# Production Environment Variables
# Note: React requires REACT_APP_ prefix for custom environment variables
# These are baked into the build at compile time

# API Base URL - Update this to match your backend deployment
REACT_APP_API_URL=http://localhost:8080/api

# You can override this during Docker build:
# docker build --build-arg REACT_APP_API_URL=https://api.production.com/api -t taskify-frontend .
